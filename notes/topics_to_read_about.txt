1. How float and double data types are stored in memory
-> the sign, exponent and significand bits


2. Accidental overwrite of values in const-qualified data

3. Associativity when prefix ++ or -- is used with pointer
-> effective C book page 63

4. Memory alignment (_Alignas(), etc.)